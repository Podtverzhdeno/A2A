server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://a1:8100;
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_read_timeout 60s;
    }

    location = /health {
        proxy_pass http://a1:8100/health;
    }

    location /docs {
        proxy_pass http://a3:8000;
    }

    location /openapi.json {
        proxy_pass http://a3:8000;
    }

    location / {
        try_files $uri /index.html;
    }
}
